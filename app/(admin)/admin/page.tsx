'use client';

// Pass-through page; the parent layout's auth check decides where to redirect:
// authed users → /admin/dashboard, unauthed → /admin/login.
export default function AdminIndexPage() {
  return null;
}
